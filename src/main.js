import {api,esc,gps} from './api.js';
const icons={leaf:'<path d="M20 4c-8-1-15 2-15 8a6 6 0 0 0 6 6c6 0 9-7 9-14Z"/><path d="M4 21 15 10"/>',home:'<path d="m3 10 9-7 9 7v10H3Z"/><path d="M9 20v-7h6v7"/>',search:'<circle cx="10" cy="10" r="6"/><path d="m15 15 5 5"/>',calendar:'<rect x="3" y="5" width="18" height="16" rx="3"/><path d="M7 2v6m10-6v6M3 11h18"/>',heart:'<path d="M20 5c-4-4-8 1-8 1s-4-5-8-1c-4 4 2 10 8 14 6-4 12-10 8-14Z"/>',help:'<circle cx="12" cy="12" r="9"/><path d="M9 9a3 3 0 1 1 4 3l-1 1m0 3h.01"/>',pin:'<path d="M19 10c0 5-7 11-7 11S5 15 5 10a7 7 0 1 1 14 0Z"/><circle cx="12" cy="10" r="2"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',bell:'<path d="M5 16h14l-2-4V9a5 5 0 0 0-10 0v3Zm5 4h4"/>',phone:'<path d="m7 3 3 5-3 3c1 3 3 5 6 6l3-3 5 3c-1 8-15 2-18-10-1-3 1-4 4-4Z"/>',check:'<path d="m5 12 4 4L19 6"/>',shield:'<path d="m12 3 8 3v6c0 5-8 9-8 9s-8-4-8-9V6Z"/><path d="m8 12 3 3 5-6"/>',users:'<circle cx="9" cy="7" r="3"/><path d="M2 21v-3a7 7 0 0 1 14 0v3m0-17a3 3 0 0 1 0 6m3 4c3 1 3 4 3 7"/>',chevron:'<path d="m9 5 7 7-7 7"/>',globe:'<circle cx="12" cy="12" r="9"/><ellipse cx="12" cy="12" rx="4" ry="9"/><path d="M3 12h18"/>'};
const ic=(name,cls='')=>`<svg class="icon ${cls}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">${icons[name]||icons.leaf}</svg>`;
const crops=[['All crops','सर्व पिके','🌱'],['Grape','द्राक्ष','🍇'],['Pomegranate','डाळिंब',''],['Onion','कांदा','🧅'],['Vegetables','भाजीपाला','🥬'],['Guava','पेरू','🍐']];
let teams=[],user=null,location=null,serviceError='',loading=true;
let selected='All crops',view='home',saved=new Set(JSON.parse(localStorage.getItem('krishi-saved-team-ids')||'[]')),bookings=[],marathi=false,query='',radius=10,watchId=null;
function render(){document.querySelector('#app').innerHTML=`<aside class="sidebar"><a class="brand" href="#"> <span class="brand-mark">${ic('leaf')}</span><span>Krishi Saathi<small>कृषी साथी</small></span></a><div class="workspace-label">YOUR FARM COMPANION</div><nav>${[['home','home','Home'],['search','search','Find labour'],['bookings','calendar','My bookings'],['saved','heart','Saved teams']].map(([id,icon,label])=>`<button class="nav-item ${view===id?'active':''}" data-view="${id}">${ic(icon)}<span>${marathi?({home:'मुख्य पान',search:'मजूर शोधा',bookings:'माझ्या बुकिंग',saved:'जतन केलेले गट'}[id]):label}</span>${id==='bookings'&&bookings.length?`<b>${bookings.length}</b>`:''}${id==='home'?'<span class="active-dot"></span>':''}</button>`).join('')}</nav><div class="sidebar-bottom"><div class="help-card"><span class="help-icon">${ic('phone')}</span><h4>A little help goes a long way.</h4><p>We’re here to help you find<br>the right farm support.</p><button id="get-help">How to book labour ${ic('arrow')}</button></div><button class="nav-item" id="help-center">${ic('help')}<span>Help & support</span></button><a class="nav-item admin-entry" href="/admin">${ic('shield')}<span>KVK admin</span></a><div class="partner"><span class="partner-symbol">✺</span><div>An initiative supported by<strong>KVK Solapur-I & MIT VU</strong></div></div></div></aside><div class="main-shell"><header><div class="location">${ic('pin')}<div><small>Your farm location</small><button id="location">${esc(location ? (user?.address || 'GPS location set') : 'Set your farm location')} <span>⌄</span></button></div></div><div class="header-right"><button class="language" id="language">${ic('globe')} ${marathi?'English':'English / मराठी'}</button><span class="header-divider"></span><button class="notification" id="notifications" aria-label="Notifications">${ic('bell')}<i></i></button><button class="profile" id="profile">${user?esc(user.name.split(" ").map(n=>n[0]).slice(0,2).join("")):"Sign in"}</button></div></header><main><div class="welcome"><div><div class="eyebrow">YOUR FARM. YOUR PEOPLE.</div><h1>${marathi?'नमस्कार':'Namaste'}, ${esc(user?.name.split(' ')[0]||'farmer')}! <span>☀</span></h1><p>${marathi?'तुमच्या शेतीसाठी योग्य मदत, जवळच.':'A helping hand for your farm is always nearby.'}</p></div><div class="today">${ic('calendar')}<div>${new Intl.DateTimeFormat('en-IN',{weekday:'long',day:'numeric',month:'long'}).format(new Date())}<small>A good day to grow together</small></div></div></div>${view==='bookings'?bookingView():`${user?.role==='provider'?`<section class="provider-banner"><div><h3>Help nearby farmers</h3><p>Publish your labour team and respond to farm requests.</p></div><button class="primary" id="provider-team">Manage my team ${ic('arrow')}</button></section>`:''}<section class="hero"><div class="hero-content"><div class="hero-label"><span></span> ROOTED IN TRUST. GROWING TOGETHER.</div><h2>${marathi?'योग्य मजूर.\nयोग्य वेळी.':'The right hands.\nAt the right time.'}</h2><p>Find trusted, skilled labour near your farm.<br>Less searching. More growing.</p><button class="primary" id="hero-search">${marathi?'मजूर शोधा':'Find labour near me'} ${ic('arrow')}</button><div class="hero-trust">${ic('shield')} Local labour teams <span>•</span> Clear daily rates <span>•</span> Local support</div></div><div class="hero-image"><div class="image-gradient"></div><div class="photo-caption"><span class="caption-icon">${ic('users')}</span><div>Good people. Better harvests.<small>Connecting farms and families in Solapur</small></div></div></div></section><div class="benefits"><div>${ic('shield')}<span><strong>People you can trust</strong><small>Provider profiles and clear booking status</small></span></div><div>${ic('pin')}<span><strong>Help closer to home</strong><small>Nearby workers, lower travel costs</small></span></div><div>${ic('users')}<span><strong>For every farm task</strong><small>Skilled hands for every season</small></span></div></div><section class="crop-section"><div class="section-heading"><div><h2>${marathi?'कोणत्या पिकासाठी मदत हवी?':'What do you need help with?'}</h2><p>Choose your crop to find people with the right skills.</p></div><span class="little-label">A little help. A big difference.</span></div><div class="crop-list">${crops.map(([name,local,emoji])=>`<button class="crop ${selected===name?'selected':''}" data-crop="${name}"><span class="crop-emoji">${emoji||'<span class="pomegranate">●<i>♛</i></span>'}</span><strong>${name}</strong><small>${local}</small>${selected===name?`<span class="crop-check">${ic('check')}</span>`:''}</button>`).join('')}</div></section><section id="teams"><div class="section-heading team-heading"><div><h2>${view==='saved'?'Your saved teams':marathi?'जवळचे उपलब्ध मजूर गट':'Available teams near you'} <span class="count">${filtered().length}</span></h2><p>Trusted hands, ready to help. Find the team that fits your farm.</p></div><button class="text-button" id="all-teams">View all teams ${ic('arrow')}</button></div><div class="filter-row"><div class="filter-buttons"><span class="available-dot"></span> Listed as available <span class="filter-separator">|</span><button id="distance">${ic('pin')} ${location?`Within ${radius} km`:`All locations`} <span>⌄</span></button></div><label class="search-field">${ic('search')}<input id="search-input" placeholder="Search a skill or team" value="${query}"></label></div><div class="teams-grid">${filtered().map(teamCard).join('')||`<div class="empty">${loading?'Loading labour teams…':serviceError?esc(serviceError):'No teams listed yet. Labour providers can sign in and publish a team.'}</div>`}</div></section><section class="support-banner"><div class="support-emblem">${ic('leaf')}</div><div><h3>Growing stronger, together.</h3><p>Choose a team, send a request, and get confirmation in My bookings.</p></div><button id="support-call">${ic('phone')} Booking help</button></section><footer><span>Made for farmers. Built on trust.</span><span>Krishi Saathi · कृषी साथी ${ic('leaf')}</span></footer>`}</main></div><div id="modal-root"></div><div id="toast" role="status"></div>`;bind();}
function filtered(){return teams.filter(t=>(selected==='All crops'||t.crops.includes(selected))&&(view!=='saved'||saved.has(t.id))&&`${t.name} ${t.skills.join(' ')}`.toLowerCase().includes(query.toLowerCase()));}
function teamCard(t){return `<article class="team-card"><div class="team-photo" style="background-image:url(https://images.unsplash.com/photo-1500382017468-9049fed747ef?auto=format&fit=crop&w=700&q=85)"><span class="team-badge green">${ic('users')} ${t.verified?'KVK verified':'Registered labour provider'}</span><button class="save ${saved.has(t.id)?'is-saved':''}" data-save="${t.id}" aria-label="Save ${esc(t.name)}">${ic('heart')}</button><span class="photo-availability"><i></i> Accepting requests</span></div><div class="team-body"><div class="team-title"><h3>${esc(t.name)}</h3></div><div class="team-location">${ic('pin')} ${esc(t.place)} <span>${t.distanceKm==null?'· Set GPS to see distance':`· ${t.distanceKm} km away`}</span></div><div class="team-rating"><span>${t.verified?'KVK verified':'Verification pending'}</span><span class="team-size">${ic('users')} ${t.people} workers</span></div><div class="skill-tags">${t.skills.slice(0,3).map(s=>`<span>${esc(s)}</span>`).join('')}</div><div class="team-bottom"><div><strong>₹${t.price}</strong><span> / person / day</span></div><button data-book="${t.id}">View team ${ic('arrow')}</button></div></div></article>`;}
function bookingView(){return `<section class="bookings-panel"><div class="section-heading"><h2>${user?.role==='provider'?'Incoming farm requests':'My bookings'}</h2><button class="text-button" id="refresh-bookings">Refresh ${ic('arrow')}</button></div><p>${user?.role==='provider'?'Accept the requests your team can fulfil.':'Your farm support requests and provider responses.'}</p>${!user?'<div class="empty"><button class="primary" id="booking-signin">Sign in to see bookings</button></div>':bookings.length?bookings.map(b=>`<article class="booking-row"><span class="booking-icon">${ic('calendar')}</span><div><h3>${esc(b.team)}</h3><p>${esc(b.task)} · ${b.workers} workers · ${esc(b.date)}</p><small>${esc(b.address)} · ₹${b.total.toLocaleString('en-IN')} / day</small><p>${user.role==='provider'?`Farmer: ${esc(b.farmerName)} · <a href="tel:${esc(b.farmerPhone)}">${esc(b.farmerPhone)}</a>`:`Provider: ${esc(b.providerName)} · <a href="tel:${esc(b.providerPhone)}">${esc(b.providerPhone)}</a>`}</p>${b.providerLocation&&user.role==='farmer'?`<p class="gps-last-update">Last team GPS update: ${new Date(b.providerLocation.updatedAt).toLocaleString('en-IN',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'})}</p><a class="gps-map-link" href="https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(b.providerLocation.lat+','+b.providerLocation.lng)}" target="_blank" rel="noopener">See team location on map ↗</a>`:''}<div class="booking-actions">${user.role==='provider'&&b.status==='accepted'?`<button class="text-button" data-share="${b.id}" data-enabled="${!b.locationSharing}">${b.locationSharing?'Stop sharing my GPS':'Share my GPS with this farmer'}</button>`:''}${user.role==='provider'&&b.status==='pending'?`<button data-status="accepted" data-id="${b.id}" class="primary">Accept</button><button data-status="declined" data-id="${b.id}" class="text-button">Decline</button>`:''}${user.role==='provider'&&b.status==='accepted'?`<button data-status="completed" data-id="${b.id}" class="primary">Mark completed</button>`:''}${user.role==='farmer'&&['pending','accepted'].includes(b.status)?`<button data-status="cancelled" data-id="${b.id}" class="text-button">Cancel request</button>`:''}</div></div><span class="pending ${esc(b.status)}">${statusText(b.status)}</span></article>`).join(''):'<div class="empty">No booking requests yet.</div>'}</section>`;}
function toast(msg){let e=document.querySelector('#toast');e.textContent=msg;e.classList.add('visible');setTimeout(()=>e.classList.remove('visible'),3500);}
function modal(content){document.querySelector('#modal-root').innerHTML=`<div class="modal-backdrop"><section class="modal"><button class="modal-close" aria-label="Close">×</button>${content}</section></div>`;document.querySelector('.modal-close').onclick=()=>document.querySelector('#modal-root').innerHTML='';document.querySelector('.modal-backdrop').onclick=e=>{if(e.target.classList.contains('modal-backdrop'))document.querySelector('#modal-root').innerHTML='';};}
function book(id){
 const t=teams.find(t=>t.id===id);if(!user)return authModal(()=>book(id));if(user.role!=='farmer')return toast('Use a farmer account to request labour.');
 const requestKey=crypto.randomUUID();modal(`<div class="modal-kicker">LOCAL LABOUR TEAM</div><h2>${esc(t.name)}</h2><p>${t.people} workers · ${t.distanceKm==null?esc(t.place):`${t.distanceKm} km away`}</p><div class="modal-rate">₹${t.price} <small>per person / day</small></div><div class="modal-skills">${t.skills.map(esc).join(' · ')}</div><form id="booking-form"><label>What work do you need?<select name="task">${t.skills.map(s=>`<option value="${esc(s)}">${esc(s)}</option>`).join('')}</select></label><div class="form-columns"><label>Work date<input type="date" name="date" min="${new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kolkata',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}" required></label><label>Number of workers<input type="number" name="workers" min="1" max="${t.people}" value="${Math.min(4,t.people)}" required></label></div><label>Your farm address<input name="address" value="${esc(user.address)}" placeholder="Village, farm name or landmark" required></label><p class="form-note">Your request goes to this provider. The booking is confirmed only when they accept it. Your name, phone and farm address are shared with the provider.</p><p class="form-error" id="booking-error" role="alert"></p><button class="primary" type="submit">Send booking request ${ic('arrow')}</button></form>`);
 document.querySelector('#booking-form').onsubmit=async e=>{e.preventDefault();const button=e.target.querySelector('button[type=submit]');button.disabled=true;const d=Object.fromEntries(new FormData(e.target));try{await api('/bookings',{method:'POST',body:{...d,teamId:t.id,workers:Number(d.workers),requestKey,...(location?{lat:location.lat,lng:location.lng}:{})}});await loadBookings();view='bookings';render();toast('Request sent to the provider. Waiting for acceptance.');}catch(err){document.querySelector('#booking-error').textContent=err.message;button.disabled=false;}};
}
function help(){modal(`<span class="modal-kicker">YOUR FARM SUPPORT</span><h2>Book labour in 3 easy steps</h2><ol class="help-steps"><li>Choose your crop and a nearby registered labour team.</li><li>Enter the work date, number of workers and farm address. Send your request.</li><li>Open My bookings. When the provider accepts, your booking shows Confirmed. You can call the provider from the booking.</li></ol><p>Team registration does not mean independent skill verification. Speak with the provider about the work before the date.</p><button class="primary" id="help-done">Got it ${ic('check')}</button>`);document.querySelector('#help-done').onclick=()=>document.querySelector('#modal-root').innerHTML='';}
function bind(){document.querySelectorAll('[data-view]').forEach(e=>e.onclick=()=>{view=e.dataset.view;query='';render();if(view==='bookings'&&user)loadBookings().then(render).catch(e=>toast(e.message));if(view==='search')document.querySelector('#teams').scrollIntoView({behavior:'smooth'});});document.querySelectorAll('[data-crop]').forEach(e=>e.onclick=()=>{selected=e.dataset.crop;render();});document.querySelectorAll('[data-save]').forEach(e=>e.onclick=()=>{const id=Number(e.dataset.save);saved.has(id)?saved.delete(id):saved.add(id);localStorage.setItem('krishi-saved-team-ids',JSON.stringify([...saved]));render();});document.querySelectorAll('[data-book]').forEach(e=>e.onclick=()=>book(Number(e.dataset.book)));document.querySelector('#language').onclick=()=>{marathi=!marathi;render();};['get-help','help-center','support-call'].forEach(id=>{const e=document.getElementById(id);if(e)e.onclick=help;});const search=document.querySelector('#search-input');if(search)search.oninput=e=>{query=e.target.value;document.querySelector('.teams-grid').innerHTML=filtered().map(teamCard).join('')||'<div class="empty">No matching teams. Try another skill.</div>';document.querySelector('.count').textContent=filtered().length;document.querySelectorAll('[data-book]').forEach(e=>e.onclick=()=>book(Number(e.dataset.book)));document.querySelectorAll('[data-save]').forEach(e=>e.onclick=()=>{let id=Number(e.dataset.save);saved.has(id)?saved.delete(id):saved.add(id);localStorage.setItem('krishi-saved-team-ids',JSON.stringify([...saved]));e.classList.toggle('is-saved',saved.has(id));});};['hero-search','all-teams','find-first'].forEach(id=>{let e=document.getElementById(id);if(e)e.onclick=()=>{selected='All crops';view='search';render();document.querySelector('#teams').scrollIntoView({behavior:'smooth'});document.querySelector('#search-input').focus({preventScroll:true});};});document.querySelector('#notifications').onclick=()=>toast(bookings.length?`${bookings.length} team request${bookings.length>1?'s':''} saved. Check My bookings.`:'You’re all caught up. No new notifications.');document.querySelector('#location').onclick=locationModal;document.querySelector('#profile').onclick=accountModal;const distance=document.querySelector('#distance');if(distance)distance.onclick=()=>{radius=radius===10?25:radius===25?50:10;loadTeams();};liveBindings();}

render();
boot();
async function loadTeams(){loading=true;serviceError='';try{const params=new URLSearchParams(location?{lat:location.lat,lng:location.lng,radius}:{});teams=(await api('/teams?'+params)).teams;}catch(e){teams=[];serviceError=e.message;}loading=false;if(!document.querySelector('.modal-backdrop'))render();}
async function loadBookings(){if(user)bookings=(await api('/bookings')).bookings;}
async function boot(){if(sessionStorage.getItem('krishi-token'))try{user=(await api('/me')).user;if(user.role==='admin'){window.location.href='/admin';return;}if(user.lat!=null)location={lat:user.lat,lng:user.lng};await loadBookings();}catch(e){serviceError=e.message;}await loadTeams();}
function statusText(status){return marathi?({pending:'उत्तराची प्रतीक्षा',accepted:'निश्चित झाले',declined:'नाकारले',cancelled:'रद्द केले',completed:'पूर्ण झाले'}[status]):({pending:'Waiting for provider',accepted:'Confirmed',declined:'Declined',cancelled:'Cancelled',completed:'Completed'}[status]);}
function liveBindings(){document.querySelectorAll('[data-share]').forEach(button=>button.onclick=()=>shareTeamLocation(Number(button.dataset.share),button.dataset.enabled==='true',button));const providerTeam=document.querySelector('#provider-team');if(providerTeam)providerTeam.onclick=teamModal;document.querySelectorAll('[data-status]').forEach(button=>button.onclick=async()=>{button.disabled=true;try{await api(`/bookings/${button.dataset.id}/status`,{method:'PATCH',body:{status:button.dataset.status}});await loadBookings();render();toast('Booking status updated.');}catch(e){toast(e.message);button.disabled=false;}});const sign=document.querySelector('#booking-signin');if(sign)sign.onclick=()=>authModal(()=>{view='bookings';render();});const refresh=document.querySelector('#refresh-bookings');if(refresh)refresh.onclick=()=>loadBookings().then(render).catch(e=>toast(e.message));}
function authModal(after=()=>render(),register=false){modal(`<div class="modal-kicker">WELCOME TO KRISHI SAATHI</div><h2>${register?'Create your account':'Sign in'}</h2><p>${register?'Choose your role to get started.':'Use your registered mobile number and password.'}</p><form id="auth-form">${register?'<label>Your name<input name="name" minlength="2" required></label><div class="form-columns"><label>Age<input name="age" type="number" min="18" max="110" required></label><label>I am a<select name="role"><option value="farmer">Farmer / शेतकरी</option><option value="provider">Labour provider / मजूर गट</option></select></label></div><label>Village / farm address<input name="address" minlength="3" required></label>':''}<label>Mobile number<input name="phone" type="tel" inputmode="numeric" pattern="[6-9][0-9]{9}" maxlength="10" placeholder="10-digit Indian number" autocomplete="tel" required></label><label>Password<input name="password" type="password" minlength="8" maxlength="128" autocomplete="${register?'new-password':'current-password'}" required></label><p class="form-error" id="auth-error" role="alert"></p><button class="primary" type="submit">${register?'Create account':'Sign in'}</button></form><button class="auth-switch text-button" id="auth-switch">${register?'Already registered? Sign in':'New here? Create an account'}</button>`);document.querySelector('#auth-switch').onclick=()=>authModal(after,!register);document.querySelector('#auth-form').onsubmit=async e=>{e.preventDefault();const button=e.target.querySelector('button');button.disabled=true;const d=Object.fromEntries(new FormData(e.target));if(register){d.age=Number(d.age);if(location){d.lat=location.lat;d.lng=location.lng;}}try{const result=await api('/auth/'+(register?'register':'login'),{method:'POST',body:d});sessionStorage.setItem('krishi-token',result.token);user=result.user;if(user.lat!=null)location={lat:user.lat,lng:user.lng};await loadBookings();render();after();}catch(err){document.querySelector('#auth-error').textContent=err.message;button.disabled=false;}};}
function accountModal(){if(!user)return authModal();modal(`<div class="modal-kicker">${esc(user.role.toUpperCase())} ACCOUNT</div><h2>${esc(user.name)}</h2><p>${esc(user.phone)} · ${esc(user.address)}</p>${user.role==='provider'?'<button class="primary" id="manage-team">Manage my labour team</button><button class="auth-switch text-button" id="provider-inbox">View booking requests</button>':''}<form id="account-form"><label>Your name<input name="name" value="${esc(user.name)}" minlength="2" required></label><label>Farm / village address<input name="address" value="${esc(user.address)}" minlength="3" required></label><p class="form-error" id="account-error"></p><button class="primary" type="submit">Save details</button></form><button class="auth-switch text-button" id="logout">Sign out</button>`);document.querySelector('#account-form').onsubmit=async e=>{e.preventDefault();try{user=(await api('/me',{method:'PATCH',body:Object.fromEntries(new FormData(e.target))})).user;render();toast('Account updated.');}catch(err){document.querySelector('#account-error').textContent=err.message;}};document.querySelector('#logout').onclick=async()=>{try{await api('/auth/logout',{method:'POST',body:{}});}catch{}sessionStorage.removeItem('krishi-token');user=null;bookings=[];location=null;if(watchId!=null)navigator.geolocation.clearWatch(watchId);watchId=null;view='home';document.querySelector('#modal-root').innerHTML='';await loadTeams();};const manage=document.querySelector('#manage-team');if(manage)manage.onclick=teamModal;const inbox=document.querySelector('#provider-inbox');if(inbox)inbox.onclick=async()=>{view='bookings';await loadBookings();render();};}
function locationModal(){modal(`<div class="modal-kicker">FIND HELP NEAR YOU</div><h2>Your farm location</h2><p>Use GPS while you are at your farm. Your precise farm location is shared only with a provider you request.</p><button class="primary" id="use-gps">${ic('pin')} Use my GPS location</button><p id="gps-status" role="status">${location?`${location.lat.toFixed(5)}, ${location.lng.toFixed(5)}`:'No GPS location set yet.'}</p><label class="tracking-toggle"><input type="checkbox" id="live-gps" ${watchId!=null?'checked':''}> Update my location while this app is visible</label><p class="form-note">Location access stops when the app is hidden. If you are away from the farm, enter its coordinates below.</p><form id="coordinates-form"><div class="form-columns"><label>Farm latitude<input name="lat" type="number" step="any" min="-90" max="90" value="${location?.lat??''}" required></label><label>Farm longitude<input name="lng" type="number" step="any" min="-180" max="180" value="${location?.lng??''}" required></label></div><p class="form-error" id="location-error"></p><button class="primary">Save farm location</button></form>`);document.querySelector('#use-gps').onclick=async e=>{e.currentTarget.disabled=true;document.querySelector('#gps-status').textContent='Finding your GPS location…';try{await saveLocation(await gps());await loadTeams();document.querySelector('#modal-root').innerHTML='';render();toast('GPS location saved. Nearby distances are now calculated.');}catch(err){const status=document.querySelector('#gps-status');if(status)status.textContent=err.message;const button=document.querySelector('#use-gps');if(button)button.disabled=false;}};document.querySelector('#coordinates-form').onsubmit=async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target));try{await saveLocation({lat:Number(d.lat),lng:Number(d.lng)});await loadTeams();document.querySelector('#modal-root').innerHTML='';render();toast('Farm coordinates saved.');}catch(err){document.querySelector('#location-error').textContent=err.message;}};document.querySelector('#live-gps').onchange=e=>{if(watchId!=null)navigator.geolocation.clearWatch(watchId);watchId=null;if(e.target.checked){if(!navigator.geolocation)return toast('GPS is not supported.');let last=0;watchId=navigator.geolocation.watchPosition(async p=>{if(Date.now()-last<30000)return;last=Date.now();try{await saveLocation({lat:p.coords.latitude,lng:p.coords.longitude});await loadTeams();}catch(err){toast(err.message);}},err=>{toast(err.message);navigator.geolocation.clearWatch(watchId);watchId=null;},{enableHighAccuracy:true,timeout:20000,maximumAge:10000});}};}
async function saveLocation(next){if(user)user=(await api('/me',{method:'PATCH',body:{lat:next.lat,lng:next.lng}})).user;location=next;}
document.addEventListener('visibilitychange',()=>{if(document.hidden&&watchId!=null){navigator.geolocation.clearWatch(watchId);watchId=null;}else if(!document.hidden&&user&&view==='bookings')loadBookings().then(render).catch(e=>toast(e.message));});
async function teamModal(){let t;try{t=(await api('/provider/team')).team;}catch(e){return toast(e.message);}let coords=t?{lat:t.lat,lng:t.lng}:location;modal(`<div class="modal-kicker">LABOUR PROVIDER</div><h2>${t?'Manage your team':'Publish your labour team'}</h2><form id="team-form"><label>Team name<input name="name" value="${esc(t?.name||'')}" minlength="2" required></label><label>Village / area<input name="place" value="${esc(t?.place||user.address)}" minlength="2" required></label><label>Crops<select name="crops" multiple size="4">${['Grape','Pomegranate','Onion','Vegetables','Guava','General farm work','Animal husbandry'].map(c=>`<option ${t?.crops.includes(c)?'selected':''}>${c}</option>`).join('')}</select></label><label>Skills (separate with commas)<input name="skills" value="${esc(t?.skills.join(', ')||'')}" placeholder="Harvesting, Pruning, Weeding" required></label><div class="form-columns"><label>Workers<input name="people" type="number" min="1" max="500" value="${t?.people||5}" required></label><label>₹ per person / day<input name="price" type="number" min="1" max="100000" value="${t?.price||450}" required></label></div><div class="form-columns"><label>Team base latitude<input name="lat" type="number" step="any" min="-90" max="90" value="${coords?.lat??''}" required></label><label>Team base longitude<input name="lng" type="number" step="any" min="-180" max="180" value="${coords?.lng??''}" required></label></div><button type="button" class="text-button" id="team-gps">${ic('pin')} Use GPS for team base</button><label class="tracking-toggle"><input type="checkbox" name="available" ${t?.available!==false?'checked':''}> Available for booking requests</label><p class="form-note">Your listing is public. Provider registration does not mean the team has been independently verified.</p><p class="form-error" id="team-error"></p><button class="primary" type="submit">Publish team</button></form>`);document.querySelector('#team-gps').onclick=async()=>{try{const p=await gps();document.querySelector('[name=lat]').value=p.lat;document.querySelector('[name=lng]').value=p.lng;}catch(e){document.querySelector('#team-error').textContent=e.message;}};document.querySelector('#team-form').onsubmit=async e=>{e.preventDefault();const button=e.target.querySelector('[type=submit]');button.disabled=true;const d=new FormData(e.target);try{await api('/provider/team',{method:'PUT',body:{name:d.get('name'),place:d.get('place'),crops:d.getAll('crops'),skills:d.get('skills').split(',').map(s=>s.trim()).filter(Boolean),people:Number(d.get('people')),price:Number(d.get('price')),lat:Number(d.get('lat')),lng:Number(d.get('lng')),available:d.has('available')}});await loadTeams();document.querySelector('#modal-root').innerHTML='';render();toast('Team published. Farmers can now send you requests.');}catch(err){document.querySelector('#team-error').textContent=err.message;button.disabled=false;}};}

async function shareTeamLocation(id,enabled,button){button.disabled=true;try{
 if(enabled){await saveLocation(await gps());}
 await api(`/bookings/${id}/location-sharing`,{method:'PATCH',body:{enabled}});
 if(enabled&&watchId==null){let last=0;watchId=navigator.geolocation.watchPosition(async p=>{if(Date.now()-last<10000)return;last=Date.now();try{await saveLocation({lat:p.coords.latitude,lng:p.coords.longitude});}catch(e){toast(e.message);} },e=>{toast(e.message);navigator.geolocation.clearWatch(watchId);watchId=null;},{enableHighAccuracy:true,timeout:20000,maximumAge:0});}
 await loadBookings();if(!enabled&&!bookings.some(b=>b.locationSharing)&&watchId!=null){navigator.geolocation.clearWatch(watchId);watchId=null;}render();toast(enabled?'GPS sharing enabled for this confirmed booking.':'GPS sharing stopped.');
 }catch(e){toast(e.message);button.disabled=false;}}
let refreshingBookings=false;
setInterval(async()=>{if(document.hidden||!user||view!=='bookings'||refreshingBookings||document.querySelector('.modal-backdrop'))return;refreshingBookings=true;try{await loadBookings();render();}catch{}finally{refreshingBookings=false;}},15000);
